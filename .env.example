# URL webhook n8n для приёма сообщений (COO).
# Примеры:
#   VITE_N8N_WEBHOOK_URL=https://n8n.example.com/webhook/abc-123
#   VITE_N8N_WEBHOOK_URL=http://localhost:5678/webhook/xxx
# Если пусто — в dev используется proxy /api (vite.config.ts).
VITE_N8N_WEBHOOK_URL=

# Секрет для HMAC-подписи запросов и ответов (body_b64 + заголовки X-Timestamp, X-Nonce, X-Signature).
# Если задан — все запросы к webhook подписываются; ответы с body_b64 проверяются.
# Для клиента (Vite) переменная должна быть VITE_A1_WEBHOOK_SECRET.
VITE_A1_WEBHOOK_SECRET=

# Домен для Traefik (docker-compose). На каком хосте отдавать приложение.
# Примеры:
#   TRAEFIK_HOST=1lab.one
#   TRAEFIK_HOST=app.mycompany.com
#   TRAEFIK_HOST=localhost
# Несколько доменов в Traefik: в docker-compose в label rule укажите вручную, например:
#   Host(`domain1.com`) || Host(`domain2.com`)
TRAEFIK_HOST=a1.localhost
A1_WEBHOOK_SECRET=super_secret