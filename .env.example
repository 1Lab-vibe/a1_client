# URL webhook n8n для приёма сообщений (COO).
# Пример: https://your-n8n.com/webhook/xxx или http://localhost:5678/webhook/xxx
# Если не задано, используется proxy /api (vite.config.ts → localhost:5678/webhook).
VITE_N8N_WEBHOOK_URL=